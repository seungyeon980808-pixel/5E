{"from_attempt":1,"to_attempt":2,"failure_tags":["EXTRA_CONTEXT"],"root_cause":"The prompt stated center alignment as an invariant but did not say that invariants are nondrawn constraints, so the model rendered an unlisted optical axis.","change":"Added a global rule that relations and invariants are constraints rather than drawable objects, plus a physics guardrail against guide axes and force/motion/optical lines.","regression_guard":"Closed inventory still explicitly permits a baseline while all unlisted guide lines remain hard-gated."}
