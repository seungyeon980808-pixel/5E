# 5E 작업 원칙

- 사용자가 명시적으로 수정을 시작하라고 지시하기 전에는 제품 코드를 변경하지 않는다. 문제 제보·질문·개선 의견·검토·기획·시안 요청은 수정 승인이 아니다. 승인은 지정된 범위에만 적용한다.
- 승인 전에는 읽기·분석·제안만 한다. 요청한 문서와 시안은 작성할 수 있지만, 시안은 제품 코드와 분리한다. ‘기억만’, ‘시안만’, ‘보류’ 지시를 지킨다.
- 요청한 부분만 수정한다. 관련 없는 리팩토링이나 기능 제거로 문제를 해결하지 않는다. 새로 발견한 문제는 보고하고 임의로 범위를 넓히지 않는다.
- 관련 기능 1~2개씩 묶어서 진행한다. 승인한 시안과 구현이 달라져야 한다면 먼저 설명하고 확인한다.
- 개발 실행창에서 확인한다. 화면 변경은 새로고침, 데스크톱 기능 변경은 재시작하며 설치본은 배포 확인 시에만 만든다.
- 변경 범위에 맞는 검증만 수행한다. 새로운 실패·변경·미해결 위험이 없으면 전체 테스트, 다중 검토, 설치 검증을 반복하지 않는다.
- 사용자가 결과를 확인하고 명시적으로 승인한 뒤에만 커밋한다. 커밋 요청은 푸시나 배포 승인이 아니다.
- 다른 세션의 변경, 실행 중인 앱, 사용자 파일과 프로필을 보존한다. 테스트는 분리한 데이터로 수행한다.
- 화면 규격은 DESIGN.md에, 보류 기능은 작업 목록에 둔다. 이 파일은 짧은 공통 원칙만 유지한다.
- 보고는 간결하게 한다. 수정 범위, 확인 결과, 남은 일과 소스·개발 실행창·설치본 중 어디에 반영됐는지 구분한다.
- 프리뷰 배포 시 버전의 Preview 바로 옆에 `[HHmm 작업 요약]`을 표시한다. 예: `1.6.0 Preview [1804 로그인 기능 수정]`. 시간은 한국 시간 기준 배포 시각이며, 작업 요약은 해당 배포에 반영한 변경을 짧게 적는다. 배포마다 갱신해 사용자가 반영된 작업을 구분할 수 있게 한다.
- 작업 완료 시 사용자가 직접 확인할 기능, 짧은 조작 순서, 각 단계의 정상 동작과 합격 기준을 반드시 안내한다. 테스트 통과 개수만으로 대신하지 않으며, 직접 검증한 내용과 사용자 확인이 필요한 내용을 구분한다.

- 이 작업트리의 개발 실행은 scripts/start-library-repair-dev.command를 사용한다. 기존 개발 프로필과 PDF 자료 경로를 유지하고, 자료 연결·검색을 확인한 뒤 사용자에게 넘긴다.
