# 튜토리얼 실사용 개선 및 검증

> **최신 후속 결과는 8~11절**: 코드 `bebe319`, 튜토리얼 59/59·에디터 84/84 통과. 코스별 UI 증거는 버전과 완주·부분·차단을 구분했다. **단일 최종 버전의 20코스 전체 정상 완주 판정은 아니다.**
> 1~7절은 최초 개선 체크포인트 기록이며 후속 현황은 아래에서 갱신한다.

## 1. 결론

2026-09-11, 기존 기초 코스를 실제 조작한 뒤 확인된 진행·안내 오류를 수정했다. 개선 기초 코스 30단계와 생성 코스 16단계를 수행했다. 기초 코스에 생성·삭제·클립보드·Undo/Redo·페이지·프로젝트 저장/열기 실습을 추가했다.

- 저장소: `seungyeon980808-pixel/5E`
- 기준 브랜치: `codex/editor-usability`
- 기준 커밋: `8335048a5d13b775fa1f55dfd6b3e897ee465f2d`
- 작업 브랜치: `codex/tutorial-improvements`
- 별도 worktree에서 작업. 원본 `Desktop/5E`의 main과 미커밋 작업을 보존했다.
- **AI 작업실·로그인·변환 알고리즘은 변경하지 않았다. 메인 병합·배포·설치도 하지 않았다.**

## 2. 실행 대상과 증거 범위

| 항목 | 실제 대상 |
|---|---|
| 소스 | 기준 커밋에서 분기한 별도 worktree |
| 웹 실행 | `http://127.0.0.1:19425/`, 위 worktree를 직접 제공한 임시 서버 |
| 앱 표시 버전 | 웹 편집기 `1.5.3` |
| 호스트 | macOS 26.6.2 (25G83), Aside Browser |
| 화면 | 브라우저 확대 110%, 앱 배율 70%, 통상 100~105%, 고배율 190% 등 |
| 직접 조작 | 실제 버튼·키보드·캔버스 마우스 입력 및 조작 후 화면 확인 |
| 단위 검사 | Node.js `test:editor`, 신규 `test:tutorial` |

**설치된 Electron 앱이나 Windows 실기 검증이 아니다.** 별도 Playwright Chromium 프로세스는 macOS 실행 권한 오류로 시작하지 못했다. 이를 Aside Browser에서 수행한 실제 UI 검증이나 제품 기능 실패와 혼동하지 않는다.

## 3. 실제 재현한 문제와 수정

| 재현한 문제 | 수정 |
|---|---|
| Space 팬 안내 중 Space가 다음 단계로 이동 | Space를 튜토리얼 전역 진행 키로 가로채지 않고 앱 팬 조작에 전달 |
| Esc 취소 안내대로 누르면 코스 자체가 종료 | 앱 취소를 우선하고 튜토리얼 종료는 명시적인 `그만` 버튼 사용 |
| Mac에서도 Ctrl 명령 표시, 각도 스냅 키도 Ctrl로 안내 | 명령은 Command(⌘), Mac 스냅은 Option(⌥)로 분리 표시. Windows는 Ctrl 유지 |
| 텍스트 확정 안내가 실제 Enter 동작과 반대 | Enter 확정/Esc 취소로 통일. 입력창과 하단 힌트도 같은 문구로 수정 |
| Command+Enter를 줄바꿈이라고 설명했으나 실제 Mac 입력에서 확인되지 않음 | 검증되지 않은 줄바꿈 약속 제거. 텍스트 키 처리 구현 자체는 변경하지 않음 |
| 선택하지 않은 도형의 첫 드래그는 이동 대신 선택만 됨 | 먼저 클릭해 선택한 다음 다시 잡아 드래그하는 순서로 안내 |
| Shift를 먼저 누르고 이미 선택한 도형을 잡으면 선택이 풀려 이동하지 않음 | 생성 코스의 이동 스냅 2단계는 `마우스로 먼저 잡은 뒤 Shift`로 안내. 도형 생성·크기 제한과 구분 |
| 기본 선 굵기 0.2mm가 기존 비교값 0.4mm와 달라, 변경 없이 통과 | 단계 진입 시 실제 값을 저장하고 이후 값 변경을 판정 |
| 생성·삭제·클립보드·Redo·페이지·파일 실습 누락 | `편집과 저장` 12단계 추가. 준비 5 + 기초 조작 13 + 편집과 저장 12 = 30단계 |
| 삭제 성공을 엉뚱한 기존 도형 삭제나 페이지 변경으로 오판할 위험 | 방금 그린 사각형 ID와 연습 페이지에 한정해 판정 |
| 저장/열기 취소나 불충분한 클립보드 판정으로 성공을 잘못 표시할 위험 | 클립보드와 파일 단계는 실제 결과를 확인하고 사용자가 `다음`으로 진행. 자동 성공을 주장하지 않음 |
| 자동 실습 버튼 재클릭·빠른 단계 전환·이전 실행 콜백의 간섭 위험 | 실행/방문 토큰, 전환 타이머 정리, 자동 실행 중복 방지, 재실행 단계 보호 |
| 설정 메뉴를 빠르게 누르면 환경 설정 클릭이 다음 단계의 대기 등록보다 먼저 발생 | 클릭뿐 아니라 실제 열린 메뉴/환경 설정의 가시성을 확인하여 이미 수행한 조작을 인식 |
| 큰 배율에서 코치 창이 슬라이더를 가리거나 버튼이 화면 밖으로 나감 | 대상 위·아래 여유 공간으로 높이를 제한하고 스크롤 및 하단 버튼 고정 적용 |
| 캔버스 코치의 고정 280px 폭이 좁은 오른쪽 패널 밖으로 나옴 | 실제 패널 사각형에 맞춘 위치·폭·높이로 도킹, 긴 버튼 줄바꿈 |

`js/text-editor.js`는 안내 문자열 2곳만, `js/tool-hint.js`는 선택·회전·텍스트 설명과 키 표시 분할만 변경했다. 도형 편집, 클립보드, 파일 저장 및 변환 알고리즘은 변경하지 않았다.

## 4. 실제 UI 검증 결과

### 기초 코스

수정 전에는 준비 5 + 기초 조작 14단계를 처음부터 완료했다. 수정 후에는 다음 흐름을 실제로 수행하여 30단계 완료 화면과 코스 완료 표시를 확인했다.

| 실습 | 결과 |
|---|---|
| 설정 진입·배율·설정 닫기 | 실제 조작 및 단계 진행 확인 |
| 화면 고정 해제·Space 팬·화면 고정 | 팬 중 튜토리얼이 넘어가지 않고 화면이 이동함을 확인 |
| 연습감 배치 | 연습 페이지에 사각형/직선 생성 확인 |
| 선택·이동·크기·회전 | 클릭 후 이동, 크기 손잡이, 회전 손잡이 조작 및 통과 확인 |
| 선 굵기·직선 끝점·화살표 종류·세 목표 겨냥 | 실제 값/도형 변화와 단계 진행 확인 |
| 사각형 직접 생성·삭제 | 실제 새 도형 생성과 제거 확인 |
| 실행 취소·다시 실행 | Mac Command+Z, Command+Shift+Z로 삭제 복원 및 재삭제 확인 |
| 복사·붙여넣기·잘라내기·재붙여넣기 | Mac 단축키 조작 후 오브젝트 변화 확인. 결과를 본 뒤 다음으로 진행 |
| 다른 페이지와 연습 페이지 왕복 | 내용이 다른 페이지로 전환되고 원래 연습 내용으로 복귀함을 확인 |
| 프로젝트 저장 | 브라우저 다운로드 대체 경로에서 실제 `.5e` 파일 생성 확인 |
| 프로젝트 열기 | 저장한 실제 `.5e` 파일을 파일 입력으로 다시 열어 페이지/오브젝트 복원 확인 |
| 마치기 | 정리 확인창에서 남겨두기, 완료된 코스 표시 확인 |

### 생성 코스 및 마지막 보완 검사

- `오브젝트 생성, 스냅, 라벨링` 16단계를 수행했다: 직선, 텍스트 Enter 확정, 삼각형, 바닥 스냅, 정사각형, 물리량 라벨, 빗면 스냅, 전체 선택, 그룹화, 그룹 이동, 마치기.
- Option을 누른 직선 생성, Esc로 도구 조작 취소 후 튜토리얼 유지, 마우스로 잡은 뒤 Shift를 누르는 이동 스냅을 실제로 확인했다.
- 생성 코스 중 확인된 설명을 수정한 뒤 새로고침하여 Enter 안내, 입력창/하단 힌트, 수정한 Shift 문구를 다시 확인했다.
- 이전, 건너뛰고 다음, 중도 그만, 재시작, 연습 페이지 남기기/이번 실행 페이지 지우기를 확인했다. 마지막 검사용 페이지 삭제 후 기존 7개 페이지가 유지됐다.
- 마지막 준비 단계 판정 수정 후 새로고침하여 빠른 메뉴 진입을 다시 실행했다. 클릭을 놓치지 않고 4/5 슬라이더 단계로 진행했다. 배율 복원, 설정 닫기, 본 실습 진입도 재확인했다.
- 기초 전체 실행 이후 변경되지 않은 후속 25단계의 결과를 활용하고, 이후 변경한 준비 단계 및 표시/배치를 새로 검사했다. 마지막 문구 수정마다 전체 20코스를 반복 실행했다는 뜻은 아니다.

### 배치 실측

- **앱 70%**: 오른쪽 패널 x=1158.59, 폭=150.50px. 캔버스 코치 x=1167.00, 폭=133.69px. 캔버스와 겹침 없음.
- **앱 190%**: 뷰포트 1309×818px. 슬라이더 y=355.31~385.71px, 코치 y=410~810px. 겹침 없음. 하단 버튼 영역 y=722.94~782.34px로 화면 안에 유지.
- 검증 후 앱 배율을 기본 100%로 복원했다.

### 실제 저장 파일

- 파일: `tutorial-qa.5e` (세션 산출물)
- 크기: 8,453 bytes
- JSON 프로젝트 버전: `0.17`
- 페이지: 6개, 각 페이지 오브젝트 수: `0, 2, 0, 0, 2, 4`
- SHA-256: `5167808ffafdee3a2aadf23be9adf36dd12faec39829656770ad83b005718a91`
- 파일 생성/JSON 읽기와 실제 재열기를 모두 확인했다. **네이티브 macOS 저장 대화상자 성공을 뜻하지 않는다.**

## 5. 자동 검사

```sh
npm run test:tutorial
npm run test:editor
git -c core.whitespace=cr-at-eol diff --check
```

- `test:tutorial`: **42/42 통과**. Mac/Windows 표시 분기, 단계 구성, 삭제/선 굵기 판정, 저장 취소 오판정 방지, 타이머/재진입/연타/중복 실행, 키 전달, 배치, 빠른 설정 진입 등을 검사한다.
- `test:editor`: **84/84 통과**. 기존 클립보드·플랫폼 단축키·문서/페이지 기록·프로젝트 상태/복구·확장자·이미지 삽입 회귀 검사다.
- diff 공백 검사 통과. 기존 CRLF 파일을 고려해 `cr-at-eol`을 사용한다.
- `test:editor:browser`: **[blocked]** 별도 Chromium 시작 시 macOS `bootstrap_check_in ... Permission denied`, `SIGABRT`. 브라우저 테스트 케이스 통과로 계산하지 않았다.

## 6. 미실시·한계

1. **Windows 실제 PC/Electron 키 입력·파일 창**: 미실시. Windows 결과는 플랫폼 분기 단위 모의 검사만이다.
2. **설치 Electron 앱/패키징/배포**: 미실시. 소스 기반 웹 QA다.
3. **네이티브 macOS 저장창**: 자동 조작 경계로 미실시. 다운로드 대체 저장과 `.5e` 재열기만 검증했다.
4. **전체 20코스 중 나머지 18코스 완주**: 미실시. 공통 단축키 표시 및 일부 공통 텍스트 설명 수정은 전체 코스 실사용 보장이 아니다.
5. **외부 이미지/이기종 앱 클립보드, 모든 크기·브라우저 조합**: 이번 직접 검증 범위가 아니다.
6. 자동 복구와 명시적인 프로젝트 파일 저장은 별개다. 튜토리얼 파일 단계는 사용자가 저장 결과를 확인해야 하며, 취소를 자동 성공으로 표시하지 않는다.

## 7. 원본 보존 및 인계

- 원본 `Desktop/5E`는 계속 main이며 `CLAUDE.md`, `docs/README.md` 수정과 `.omo/`, 인계 문서, `docs/research/` 미추적 작업이 그대로 남아 있다.
- 원본에서 reset/stash/checkout/clean을 하지 않았다.
- 변경은 작업 브랜치만 커밋·푸시한다. 메인 병합과 배포는 별도 승인 대상이다.
- 검증용 임시 서버는 종료하고 포트 listener 부재까지 확인한다. 백그라운드 구현이나 서비스가 계속 실행 중인 것으로 인계하지 않는다.
- 실제 다음 검증 우선순위: Windows 설치 대상에서 동일 기초 흐름과 네이티브 저장/열기 확인. 나머지 코스 완주는 별도 범위로 진행한다.

## 8. 후속 실행 기준과 자동 검사

- 후속 코드 커밋: `bebe319` (이후 문서 커밋은 코드 변경 없음).
- UI 실행 증거는 한 단일 HEAD로 합산하지 않는다. 아래 표처럼 실행 당시 커밋별로 분리한다.
  - `f44cb74`: 기초·생성 재실행, 기출 검색/이미지 경로 확인을 포함한 일부 UI 증거.
  - `8d4b721`: P1·P2 수정본 재검증의 시작 HEAD. 시작 시 clean이었으나 종료 시 범위 밖 동시 수정 2파일이 dirty였으므로, 이 UI 증거는 시작 시 로드한 런타임에만 귀속한다.
  - `b8a9f36`: P6·P7·P9 수정본 재검증 및 심화 1·2의 차단/부분 증거.
  - `55c3477`: 심화 5 그래프 코스 완주 증거.
  - `3f8263c`: 심화 6 주석 코스 9단계 스킵 0 완주 및 코치 배치 수정 확인. 심화 2의 채우기·무늬·정렬도 직접 통과한 뒤 후속 재진입·대상 선택 결함을 추가 재현했다.
  - `bebe319`: 심화 2의 직선만 선택·모달 재열기 흐름을 추가 수정하고 단위 검사를 통과했다. 이 커밋의 심화 2 전 구간 UI 완주는 아직 미검증이다.
- 자동 검사(현재): `test:tutorial` **59 통과**, `test:editor` **84 통과**, `git -c core.whitespace=cr-at-eol diff --check` 통과.
- 자동 검사는 UI 완주 증명이 아니며, 아래 UI 판정과 별도로 유지한다.


### 후속 수정 사항

- 공통 OPTICS/CIRCUIT 도구 상태뿐 아니라 정확한 심볼 ID와 생성 속성을 검사한다.
- P1 물체 두 개, P1/P2/P6 드래그 배치, P7 두 점 라벨러, P3 세 점 각도 시연을 실제 도구 방식에 맞췄다.
- P9는 자유 그리기가 아닌 실제 좌표/함수 모달로 진입한다.
- 그래프는 실제 칩과 `funcgraph.planeId/expr` 구조로 두 함수를 확인한다. 그룹은 F로 기존 그래프를 편집하며, 다음 코스에 필요한 페이지는 남긴다.
- 주석은 편집 중 목록과 최종 저장 배열을 구분하고 같은 그래프 ID에 저장됐는지 확인한다. 코치는 미리보기와 배치 버튼을 가리지 않도록 배치한다.
- 기초 마지막 및 실습 마지막의 캔버스 차단을 해제했다.
- 기출 글자는 조각 개수 감소만으로 완료 처리하지 않는다. 화살표 조각 삭제와 명시적인 글자 확인이 필요하다. 인물/글자를 위치만으로 자동 분류하는 위험한 규칙은 추가하지 않았다.
- 심화 도형은 특정 연습 사각형의 채우기·무늬만 검사한다. 간격은 실제 선 끝점으로 비교하며, 적용 버튼과 모달 재진입을 열어 두고 직선만 대상으로 준비한다.
- P10은 실제 실습 범위인 비교 틀 만들기로 설명을 정직하게 좁혔다. 주석 코스 마지막도 전체 심화 완료가 아닌 해당 연습 완료로 표현했다.

## 9. 핵심 20코스 실제 UI QA 현황

선택기 `tutorial.js`의 필터 기준으로 기본 3, 심화 6, 실습 과제 P0~P10의 11개, 합계 20코스를 기록한다. raw `COURSES`에는 레거시 숨김 코스 `TRIM_EXAM`·`ALIGN_SPACE`·`TERRAIN_COURSE`도 남아 총 23개지만, 선택기 3/6/11에는 포함되지 않는다. `미검증`은 앱 결함으로 단정하지 않는다.

|번호|코스|판정|실제 확인 범위 및 남은 한계|UI 증거 커밋|
|---:|---|---|---|---|
|1|기본 설정, 오브젝트 기초 조작|30단계 수행·말미 결함|30단계 재실행 완료. 마지막 자유조작 단계에서 dim이 캔버스를 차단하는 결함을 재현했다. 해당 마지막 단계는 수정했으나 수정 후 직접 조작 재검증은 아직 하지 않았다.|`f44cb74`|
|2|오브젝트 생성, 스냅, 라벨링|실제 완주|16단계 재실행 완료.|`f44cb74`|
|3|기출문항 라이브러리, 이미지로 내보내기|부분|검색 결과 58→20→1로 좁히기, sparse-checkout의 PNG 404 원인 확인 및 원본 1장 복원, 실제 객체화·삽입·그룹 풀기·왼쪽 삭제까지 확인. 글자 `C`가 남아도 다음 저장 단계로 넘어가는 false-pass 재현. 개인 저장·내보내기는 미검증이다.|`f44cb74`|
|4|1 · 역학 그림의 선과 치수선|차단|`b8a9f36`에서 튜토리얼 버튼을 눌러도 선택기가 열리지 않아 첫 실제 단계에 진입하지 못했다. 이전 `f44cb74`의 꺾은선 시도는 통과하지 못한 부분 수행 기록일 뿐이다.|`b8a9f36`|
|5|2 · 도형 편집과 전체 통일|부분|`3f8263c`에서 사각형·타원·삼각형 생성, 자르기, 같은 사각형 채우기·헤칭, 직선 3개 생성·가로 정렬까지 직접 통과. 정렬 적용 후 모달이 닫히며 다음 안내가 사라졌고, 수동 재열기 후 전체 객체 간격 통일은 직선 간격을 동일하게 만들지 못했다. `bebe319`에서 직선만 선택·재열기 안내를 수정하고 단위 검사했다. 회전·속성 복사 및 최종 코드 전 구간 완주는 미검증.|`b8a9f36`, `3f8263c`|
|6|3 · 이미지 요소 추출과 배열하기|부분|라이브러리 검색·삽입·복제·전체 선택과 `마치기`는 수행. 필요한 부분 영역 지정과 가로 정렬·간격 통일의 실제 적용은 미검증이다.|`f44cb74`|
|7|4 · 여러 페이지와 프로젝트 복원|부분|새 페이지 추가·도형 생성은 확인. 이미지 내보내기, 모든 페이지 내보내기, 프로젝트 저장·불러오기는 네이티브 파일 단계로 미실시/미검증이다.|`f44cb74`|
|8|5 · 좌표평면과 함수 그래프|실제 완주|7단계, 스킵 0. 빈 함수 상태에서 대기, `sin(x)`·`cos(x)` 두 칩 유지, 같은 좌표평면에 연결된 함수 그래프 2개 저장 및 `마치기` 뒤 페이지 보존 확인.|`55c3477`|
|9|6 · 그래프 표시 요소와 주석|실제 완주|9단계, 스킵 0. 그룹 선택 후 `F` 편집, 표시점·수선·화살표·라벨을 실제 미리보기 클릭하고 Apply했다. 동일 graph object `obj_mtwq97ij_1` ID 유지 확인 후 부모가 실제 `마치기`를 눌러 ‘6 코스를 마쳤습니다’와 심화 2/6 집계를 확인했다.|`3f8263c`|
|10|P0 · 빗면과 마찰 구간 (시범)|부분|실제 `EXAM_TASK_INCLINE` 코스다. 레거시 숨김 `TERRAIN_COURSE`와 다른 코스이며, 전체 최종 실습 완주 증거는 없다.|`f44cb74`|
|11|P1 · 도르래 두 물체|실제 완주|수정본에서 10/10, 스킵 0: 직선·도르래·m1·m2 생성과 m2 실제 이동 후 `마치기` 확인. 줄 연결의 과학적 완성도는 별도 미검증이다. 시작 `8d4b721` clean, 종료 시 범위 밖 2파일 dirty 관찰로 이 증거는 시작 런타임에 한정한다.|`8d4b721`|
|12|P2 · 용수철 진동|실제 완주|수정본에서 8/8, 스킵 0: 클램프·용수철·추 생성, 마지막 추 실제 이동과 직선·점선 조작 확인 후 `마치기`. 물체 간 정렬·결합의 과학적 완성도는 별도다. 시작 `8d4b721` clean, 종료 시 범위 밖 2파일 dirty 관찰로 이 증거는 시작 런타임에 한정한다.|`8d4b721`|
|13|P3 · 단진자와 각도|부분|단진자 선택까지만 실제 확인. 배치·기준선·각도 표시·Shift 이동·최종 완성은 미검증이다.|`f44cb74`|
|14|P4 · 복합 경사면 활강|부분|꺾은선 지형 생성과 다음 물체 선택 단계 전환까지 확인. 물체·Shift 이동·최종 완성은 미검증이다.|`f44cb74`|
|15|P5 · 직류 회로|부분|직류전원·저항·전구의 정확한 선택, 전원 배치 및 전구 단계 도달까지 확인. 전구 정확 배치, 도선 연결, 최종 완성은 미검증이다.|`f44cb74`|
|16|P6 · 볼록렌즈 상 작도|실제 완주|수정본 8/8, 스킵 0. 광축, 볼록렌즈, 물체의 실제 선택·대각선 드래그·`마치기` 확인. 후속 안내의 광선은 정의된 단계 요구 밖이다.|`b8a9f36`|
|17|P7 · 정상파 실험|실제 완주|수정본 6/6, 스킵 0. 정상파 선택·드래그, 라벨러 두 점 클릭+Enter, `마치기` 확인. 후속 자로 L 표시는 정의된 단계 요구 밖이다.|`b8a9f36`|
|18|P8 · 점전하 전기력선|실제 완주|6단계, 스킵 0. 전기력선 선택·생성, 곡선 네 점과 Enter 확정, 점선 적용 및 `마치기` 확인.|`f44cb74`|
|19|P9 · v–t 그래프 문항|실제 완주|수정본 6/6, 스킵 0. 실제 좌표/함수 생성 모달, 꺾은선 네 점+Enter, `마치기` 확인. 면적 빗금은 정의된 단계 요구 밖이다.|`b8a9f36`|
|20|P10 · 비교 그림의 틀 만들기|부분|정의된 4단계에서 사각형 1개를 실제 선택·생성하고 마지막 안내까지 도달했으나, 주장한 기출 가져오기·편집·원본/변형 비교 실습은 코스에 없었다. 이후 실제 페이지 복제는 확인했으나, 일괄 내보내기는 폴더 연결 요구로 차단됐고 파일은 생성하지 않았다.|`f44cb74`|

### 기본 3 이미지 자산 경로 보정과 false-pass 증거

sparse-checkout에 PNG가 빠져 실제 요청이 404가 되는 것을 확인했다. `git restore --ignore-skip-worktree-bits`로 QA 폴더에서 원본 PNG 1장을 복원한 뒤, 실제 UI에서 객체화·삽입·그룹 풀기·왼쪽 삭제를 수행했다. 그러나 글자 `C`가 남은 상태에서도 다음 저장 단계로 진행됐다.

- 화면 증거: `qa-exam-letter-false-pass.png`
- 확인한 실제 이미지 식별자: `obj_mtwpfmc4_vecimg81`
- 이 결과는 저장 또는 내보내기 성공 증거가 아니다. 해당 후속 단계들은 미검증으로 남긴다.

### 심화 6 코치 배치 보정 확인

`3f8263c`에서 코치가 곡선 미리보기를 덮는 문제를 보정했다. 실제 측정값은 `coach.right=531.99 < preview.left=552.04`, `markerControl.bottom=191.60 < coach.top=541.99`로, 코치와 미리보기/표시점 제어가 겹치지 않았다.

## 10. 아직 주장하지 않는 범위

1. **단일 최종 커밋에서 20코스 전체를 실제 완주한 기록은 없다.** 서로 다른 커밋의 제한된 UI 재검증을 합산한 표이며, 각 행의 커밋을 넘어 일반화하지 않는다.
2. 기초 코스 마지막 자유조작 단계의 dim 캔버스 차단은 수정했지만, 이 마지막 단계의 수정 후 직접 조작 재검증은 아직 없다.
3. Windows 실제 PC, 설치된 Electron 앱, 설치본의 파일 대화상자와 네이티브 macOS 파일 대화상자는 미검증이다.
4. 원본 main 보존은 기존 보고서의 범위이며, 이번 후속 작업에서 main 병합, 배포, 패키징은 수행하지 않았다.
5. 기본 3의 개인 저장·내보내기와 심화 4의 파일 저장/불러오기는 실제 파일 시스템 결과까지 확인하지 않았다. P10의 일괄 내보내기는 폴더 연결 요구에서 멈췄으며, 파일 생성은 확인하지 않았다.
6. 심화 2의 후반 회전·속성 복사와 최종 코드 전체 흐름은 미검증이다. P3·P4·P5 수정본 재시도는 해당 실행의 새 탭 연결 오류로 차단됐으며, 앞선 `f44cb74` 부분 기록을 최종 코드 통과로 확대하지 않는다.

## 11. 인계용 결론

현재 증거는 자동 검사 59/84 통과와 코스별 실제 UI 재검증을 구분해 보관한다. 정의된 실습 완주 기록은 기본 2, 심화 5·6, P1·P2·P6·P7·P8·P9에 있다. 기본 1은 30단계를 수행했으나 마지막 자유조작의 결함이 있었고, 심화 2는 후반 미검증으로 부분 상태를 유지한다. 나머지는 표의 부분·차단·미검증 상태를 유지하며, 특히 전체 20코스가 단일 버전에서 정상 완주됐다고 표현하지 않는다.
