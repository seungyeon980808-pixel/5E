# 남은 작업 브리핑 — 2026-07-04 세션 종료 시점

> 현재 HEAD: `work-dev` 브랜치 `0c7fc5d`, **v0.44.3**, GitHub push 완료, 워킹트리 깨끗.
> 다음 세션은 이 문서 + `docs/HANDOFF_20260704.md`를 같이 보면 이어서 진행 가능.

---

## ✅ 2026-07-05 후속 세션 처리 결과 (아래 §1의 3항목 전부 완료)

> **§1의 "판단 필요 3항목"은 2026-07-05 세션에서 모두 처리됨.** 요약:
>
> - **§1-2 전체 QA → 실행 완료.** v0.44.0↔v0.44.3 픽스처 렌더 SHA-256 비교 중
>   **실제 회귀 버그 1건 발견·수정**: `js/svg-assets.js`의 `CART_SVG_BASE64`가
>   v0.44.1 선언화 리팩토링 때 곡선 세그먼트(43자) 유실 → 역학 수레 모서리가
>   각지게 렌더되던 문제. v0.44.0 원본값으로 복원. **문서 오류 1건도 수정**:
>   `QA_VISUAL_CHECKLIST`의 G1/G2/C5/E5가 옛 정책(v0.36.3) 기준이라 현재
>   정책(v0.36.6, 사각형 quantity=이탤릭)과 어긋난 것을 갱신(코드는 정상).
>   → 커밋 `527ff67`, 버전 **v0.44.4**.
> - **§1-3 릴리스 태그 → 완료.** 위 QA 통과 후 **`v0.44.4` 태그** 생성·push.
> - **§1-1 숨은 기능 노출 → 최소 범위로 결정·구현 완료.** 9건 중 임팩트 대비
>   비용이 가장 낮은 **Ctrl+F 오브젝트 검색만 UI 노출**(좌측 패널 상단에
>   "🔍 검색 [Ctrl F]" 버튼 추가 → 기존 검색 모달과 연결). 나머지 8건은
>   백로그 유지(§1-1 표 참고). → 새 기능이므로 버전 **v0.45.0**.
>
> **2026-07-05 세션 종료 시점 HEAD: v0.45.0** (커밋·push·문서갱신 완료).
> 미실시로 남긴 것: QA 체크리스트 중 인라인 편집·타이핑·목업임포트 계열
> (A4-10, B3-7, D, F, G3-10) — 자동화 한계로 다음 세션에서 수동 확인 권장.
> 단 해당 파일들은 리팩토링 기간에 로직 변경이 없어 회귀 위험 낮음.

## ✅ 2026-07-05 후속 세션(2) 처리 결과

> 위에서 미실시로 남긴 QA 항목(A4-10·B3-7·D1-7·F1-7·G3-10) 전부 실행 완료 — 전 항목 Pass.
> 상세는 `QA_VISUAL_CHECKLIST_20260701.md` 참고. 실제 결함 1건 발견: G6(유니코드 로마 숫자
> Ⅰ Ⅱ 세리프 표시)이 코드상 미구현 — `state.js`의 `splitRomanRuns`는 ASCII I/II/III("구간" 뒤)만
> 처리, 유니코드 코드포인트(U+2160~)는 어디서도 검사 안 함. **사용자 결정: 코드 변경 없이 문서만
> 정정**(G5의 ASCII 경로로 실사용상 충분히 대체됨, 우선순위 낮음). 코드 변경 없으므로 버전 범프 없음.
>
> **숨은 기능 백로그 8건(§1-1 표: Alt+P, Shift+C/V, K, PageUp/Down, 방향키, F2, 텍스트 자동변환)
> → 사용자 결정: 전부 스킵 유지.** 이번 세션에서 재확인만 하고 노출하지 않기로 함(불편이 실제로
> 느껴질 때 개별 처리). 다음 세션에서 다시 꺼낼 필요 없음 — 결정 완료로 간주.

---

## 1. 지금 당장 판단이 필요한 것  ← (전부 처리됨, 위 요약 참고. 이력 보존용으로 원문 유지)

### 1-1. 숨은 기능 노출 — 범위 미확정 (9건 백로그)
지난 조사에서 11건 발견 → 2건은 이미 처리(기본값 모달 안내 문구 / 그룹화 UI는 이미 있었음 확인).
**나머지 9건은 우선순위를 못 정했음**(질문에 응답 없어 최소 범위로 진행함):

| 우선순위(체감) | 기능 | 현재 상태 |
|---|---|---|
| ~~중~~ **완료** | Ctrl+F 오브젝트 검색 모달 | **2026-07-05 노출 완료(v0.45.0)** — 좌측 패널 상단에 "🔍 검색 [Ctrl F]" 버튼 추가, `js/search.js`에서 기존 open()과 연결. 나머지 표는 미처리 백로그 |
| 낮 | Alt+P 내보내기 단축키 | 동작하지만 메뉴 툴팁에 표기 안 됨 |
| 낮 | Shift+C/Shift+V (각도 복사/붙여넣기) | 완전히 구현됨, UI 힌트 없음 |
| 낮 | K (잠금 토글) | 인스펙터에 체크박스로 이미 있음, 단축키만 미표기 |
| 낮 | PageUp/PageDown (z-order / 회전 모드에선 5도씩 회전) | 힌트 없음 |
| 낮 | 방향키 (넛지, Ctrl+방향키 큰 이동, 회전모드에선 반전) | 힌트 없음 |
| 낮 | F2 (텍스트 편집) | 더블클릭/우클릭으로 이미 발견 가능, F2는 덤 |
| 낮 | 텍스트 자동변환(theta/lambda/roman1 등 타이핑 시 기호 치환) | 팔레트 버튼으로 같은 결과를 얻는 경로 있음, 다만 설명 없이 자동 치환되는 게 당황스러울 수 있음 |

**판단 필요**: 이 중 뭘 지금 하고 뭘 버릴지(또는 전부 스킵) 결정 필요. 개인적으로는
Ctrl+F 검색 아이콘 하나 추가하는 것 정도만 임팩트 대비 비용이 낮다고 봄. 나머지는
사용 중 불편함이 실제로 느껴질 때 처리해도 될 정도로 낮은 우선순위.

### 1-2. 전체 QA — 미실행
- `docs/qa-fixtures/all_types_v0.15.json`(18타입 22객체) 로드 후 렌더 회귀(SHA-256 비교) — **리팩토링 이후 마지막 검증이 v0.44.0대**, 이후 v0.44.1~v0.44.3 변경분(SVG 자산 선언화, 단축키 가드, 디버그 게이팅)은 회귀 안 돌려봄.
- `docs/QA_VISUAL_CHECKLIST_20260701.md`(라벨러·각도호·글꼴·선택영역 내보내기) — 이것도 v0.36대 이후 미실행.
- 리스크 평가: 이번 세션 변경은 전부 **키보드 이벤트 가드 추가 / 콘솔·디버그 코드 게이팅 / 도구 id 리네임**이라 렌더링·저장 스키마에는 손 안 댐 → 회귀 가능성은 낮다고 보지만, "정식 QA를 안 돌렸다"는 사실 자체는 남아있음.

### 1-3. 릴리스 태그 — 사용자 판단
- CLAUDE.md 원칙: 태그는 "의미있는 완성 시점에만". 현재 태그는 `v0.32.0`, `v0.40.0`만 존재.
- v0.44.3은 리팩토링 3건(render/inspector/tools 분리) + Day4(이음새 계약, SVG자산 선언화) + Day5(단축키 버그 2건, 디버그 게이팅) 완료 시점 — **태그 후보로 보이나, 전체 QA를 안 돈 상태에서 태그를 찍을지는 사용자 판단.**

---

## 2. 확정 완료된 것 (재작업 불필요)

- **Day 4 (A)**: `docs/IMAGE_OBJECT_SEAM_CONTRACT.md` — image-dev 조율 문서. image-dev의 `image-objectify.js`가 이 계약을 우회 중이라는 사실도 문서에 기록해둠(강제 아님, 차기 정리 권장 사항).
- **Day 4 (B)**: `svg-assets.js` 선언화(`defineAsset()` 헬퍼) + `templates.js` 아이콘 제네릭 폴백.
- **Day 5**: 단축키 충돌 2건(F, Shift+G) 수정 + R 명명 정리 + 디버그 오버레이 게이팅(`_APP_DEBUG_ENABLED`) + SNAP-6a 로그 제거 + 기본값 모달 안내 문구.
- 전부 브라우저에서 실검증(state 직접 주입 테스트 포함) 후 push 완료.

---

## 3. 이번 세션에서 발견한 프로세스 이슈 (사소하지만 기록)

- `css/style.css`·`css/inspector.css`는 `?v=` 캐시버스트가 없음 — CSS만 바꾸면 하드 리프레시 필요(JS는 자동 갱신). 지금은 안 고침, 필요하면 별도 작업.
- **서브에이전트 조사 결과에 오탐 1건 있었음**: "그룹화/그룹해제 UI가 없다"는 조사 결과가 틀렸음 (`js/inspector/section-group.js`에 이미 구현·마운트돼 있었음, 조사 에이전트가 분리된 모듈 파일을 못 찾은 것). 실행 전 직접 재확인해서 중복 구현은 막았음 — 향후 조사 결과도 "사실로 가정하지 말고 재확인" 원칙 유지 필요.

---

## 4. 완전히 손 안 댄 것 (로드맵상 남은 큰 덩어리)

- **프론트엔드 개선**: 반드시 별도의 짧고 신선한 세션에서(스크린샷→수정 반복 루프라 긴 세션이면 토큰 급증).
- **image-dev 병렬 세션**: §4 계약대로 PNG→v1 JSON→기존 임포터 전환 여부는 image-dev 쪽 판단.
